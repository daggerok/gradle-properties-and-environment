buildscript {
    ext {
        propertiesPluginEnvironmentNameProperty = 'env'
        environmentFileDir = 'env'
    }
}

plugins {
    id "net.saliman.properties" version "1.4.6"
}

configure(allprojects) {
    group 'daggerok'
    version '0.0.1'
}

println("root-project $project.name: $prop")
